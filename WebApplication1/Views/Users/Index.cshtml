@model List<WebApplication1.Users>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
   @Html.ActionLink("Create New", "EditOrCreate", "Users")
</p>
<table class="table">
    <tr>
        <th>
            FIO
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditOrCreate", new { id=item.Id }) |
            @Html.ActionLink("Delete", "EditOrCreate", new { id=item.Id })
        </td>
    </tr>
}

</table>
